libstrong_types_core = static_library(
    'strong_types', # library name
    include_directories : inc, # previously declared include directories in root :code:`meson.build`
    install : false)

libstrong_types = [libstrong_types_core]         # combine both the static libraries
